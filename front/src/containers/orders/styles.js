import { StyleSheet } from 'react-native';
import { Metrics } from '../../themes';

export default StyleSheet.create({
  container: {
    height: '100%',
  },
  listContainer: {
    padding: Metrics.m10,
  },
});
